const state={
    screenWidth:0,
};
const getters={

};
const actions={

};
const mutations={
    SET_SCREEN_WIDTH(state,size){
        state.screenWidth=size;
    }
};
export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
